<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Add a Book</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <link rel="stylesheet" type="text/css" href="/style.css">
</head>
<body class="vh-100 d-flex flex-column">
    <!-- Page Heading -->
    <h1 class="text-center mt-3">Add a Book to Your Librarium</h1> <!-- Centered text with margin-top -->

    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item"><a href="/">Home</a></li> 
          <li class="nav-item"><a href="/books">Books</a></li>
           <% if (user) { %>  
            <li class="nav-item"><a href="/logout">Logout</a></li>  
          <% } else { %>  
            <li class="nav-item"><a href="/login">Login</a></li> 
          <% } %>
        </ul>
    </nav>

    <!-- Centered Form Container -->
    <div class="container flex-grow-1 d-flex justify-content-center align-items-center">
      <div class="card w-50"> <!-- Bootstrap card with 50% width -->
        <div class="card-body">
          <form action="<%= book._id ? '/books/' + book._id + '/edit' : '/add-book' %>" method="post">
            <!-- Form Group for Title -->
            <div class="form-group">
              <label class="form-label" for="title" style="font-weight: bold;">Title:</label>
              <input type="text" id="title" name="title" class="form-control" value="<%= book.title || '' %>">
            </div>
      
            <!-- Form Group for Author -->
            <div class="form-group">
              <label class="form-label" for="author" style="font-weight: bold;">Author:</label>
              <input type="text" id="author" name="author" class="form-control" value="<%= book.author || '' %>">
            </div>
      
            <!-- Form Group for Date Read -->
            <div class="form-group">
              <label class="form-label" for="date_read" style="font-weight: bold;">Date Read:</label>
              <input type="date" id="date_read" name="date_read" class="form-control" value="<%= book.date_read ? new Date(book.date_read).toISOString().split('T')[0] : '' %>">
            </div>
      
            <!-- Form Group for Comment -->
            <div class="form-group">
              <label class="form-label" for="comment" style="font-weight: bold;">Comment:</label>
              <textarea id="comment" name="comment" class="form-control"><%= book.comment || '' %></textarea>
            </div>
      
            <!-- Submit Button -->
            <div class="text-center mt-4">
              <button type="submit" class="btn" style="background-color: #803D3B; color: white;">
                <%= book._id ? 'Update Book' : 'Add Book' %>
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
</body>
</html>
